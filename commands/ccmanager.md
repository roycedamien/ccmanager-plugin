---
description: "Start ccmanager local UI server"
allowed-tools: ["Bash(node:*)"]
---

# Start ccmanager UI

Start the local ccmanager web UI server (default: http://127.0.0.1:7337).

```!
node "${CLAUDE_PLUGIN_ROOT}/server/index.js"